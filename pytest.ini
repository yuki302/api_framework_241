[pytest]
addopts = -vs --alluredir=./temps --clean-alluredir

markers =
    api
    ui
    ut
    e2e

log_file = ./logs/pytest.log
log_file_level = info
log_file_format = %(levelname)-8s %(asctime)s [%(name)s:%(lineno)s] :%(message)s
log_file_date_format = %Y-m-%dv %H:%M:S